<%= form_for(chatchannel) do |f| %>
  <% if chatchannel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chatchannel.errors.count, "error") %> prohibited this chatchannel from being saved:</h2>

      <ul>
      <% chatchannel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
